## Install Typescript (Global Install)
    ```
    npm install -g typescript --save-dev
    ```

## Create config file
  ```
  npx tsc --init
  ```

## Run Typescript File
  ```
  # make sure ts-node is installed before use

  npx ts-node <filename>
  ```